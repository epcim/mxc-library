{% for item in get_var('overlays.netbird.resources', []) %}
{% set defaults = load_template('../../../../schema/netbird.io/nbresource.defaults.yml') | from_yaml %}
{% set cr = merge_dict(defaults, item) %}
---
{{ cr | to_yaml }}
{% endfor %}
